{
  "name": "@forge/connector-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Forge Connector Marketplace — Browse, install, and publish connectors",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@forge/types": "workspace:*",
    "@forge/connector-sdk": "workspace:*",
    "@forge/connector-registry": "workspace:*",
    "@prisma/client": "^6.0.1",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1"
  }
}
