{
  "name": "@forge/custom-connector",
  "version": "0.1.0",
  "private": true,
  "description": "Forge Custom Connector — Create connectors visually without code",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/axios": "^4.0.0",
    "@forge/types": "workspace:*",
    "@forge/connector-sdk": "workspace:*",
    "axios": "^1.7.9",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1"
  }
}
