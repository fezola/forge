{
  "name": "@forge/config-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Forge Configuration Engine — Centralized configuration and secret management for every engine in Forge.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "@nestjs/event-emitter": "^2.1.1",
    "@forge/types": "workspace:*",
    "@forge/config-types": "workspace:*",
    "@forge/resource-types": "workspace:*",
    "@forge/workflow-types": "workspace:*",
    "rxjs": "^7.8.1",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1"
  }
}