{
  "name": "@forge/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Forge Client SDK — TypeScript client for consuming the Forge API",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@forge/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1"
  }
}
