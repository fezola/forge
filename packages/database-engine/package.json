{
  "name": "@forge/database-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Forge Database Engine — Multi-tenant database provisioning and query execution",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "@prisma/client": "^6.0.1",
    "@forge/types": "workspace:*",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "jest": "^29.7.0",
    "prisma": "^6.0.1",
    "rimraf": "^6.0.1"
  }
}
